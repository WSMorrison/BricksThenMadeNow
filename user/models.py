from django.db import models
from django.contrib.auth.models import User
from product.models import Item, Sku

# class Siteuser(models.Model):
    # siteuser = models.OneToOneField(User, on_delete=models.CASCADE)
